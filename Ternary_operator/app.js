// Print 'you can drive' or 'you cannot drive' based on your age being grater than 18 using ternary operator

const drivingAge = (age) => {
  return age > 18 ? 'You can drive' : 'You cannot drive';
};
console.log(drivingAge(3));
