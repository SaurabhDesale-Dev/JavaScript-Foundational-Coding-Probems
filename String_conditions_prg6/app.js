//JavaScript: Create a new string from a given string with the first character of the given string added at the front and back
let mainStr = 'a';
let str1 = mainStr.charAt(0);

let newStr = str1 + mainStr + str1;
console.log(newStr);
