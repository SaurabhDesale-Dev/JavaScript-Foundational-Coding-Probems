//Given a string, return true if its length is even or false if the length is odd.

let stringCondition = (str) => {
  return str.length % 2 === 0 ? true : false;
};

console.log(stringCondition('saurab'));
