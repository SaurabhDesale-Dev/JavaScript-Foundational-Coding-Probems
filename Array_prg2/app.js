//JavaScript: Compute the sum of three elements of a given array of integers of length 3

const array = [2, 3, 4];

let sum = 0;
array.forEach((e) => {
  sum = sum + e;
});
console.log(sum);
